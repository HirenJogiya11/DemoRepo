export declare type SingleFocusDispatcherListener = (key: string) => void;
export declare class SingleFocusDispatcher {
    private _listeners;
    notify(key: string): void;
    listen(listener: SingleFocusDispatcherListener): void;
}
