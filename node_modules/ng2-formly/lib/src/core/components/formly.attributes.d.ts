import { ElementRef, Renderer, OnInit, OnChanges, SimpleChanges } from '@angular/core';
import { SingleFocusDispatcher } from '../services/formly.single.focus.dispatcher';
import { FormlyFieldConfig } from './formly.field.config';
export declare class FormlyAttributes implements OnInit, OnChanges {
    private renderer;
    private elementRef;
    private focusDispatcher;
    field: FormlyFieldConfig;
    formControl: any;
    private attributes;
    private statements;
    onFocus(): void;
    constructor(renderer: Renderer, elementRef: ElementRef, focusDispatcher: SingleFocusDispatcher);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
